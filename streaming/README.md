# streaming

Новый репозиторий `streaming` создан в виде директории в текущем проекте.

## Быстрый старт

```bash
cd streaming
git init
```

После этого можно добавлять файлы и публиковать репозиторий в удалённый origin.
